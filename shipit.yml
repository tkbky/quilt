ci:
  require:
    - ci/circleci

deploy:
  override:
    - rm -fr node_modules
    - YARN_PRODUCTION=false yarn install
    - YARN_PRODUCTION=false node_modules/.bin/lerna bootstrap
    - ls -lr node_modules

